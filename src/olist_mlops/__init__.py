"""Olist late-delivery prediction: inference-side package (Task 3).

Training happens in notebooks 01-06. This package loads the fitted
artifacts those notebooks produced (preprocessor, model, feature list)
and reproduces their prediction-time feature engineering exactly, so
the API and notebook outputs match on the same input.
"""

__version__ = "1.0.0"
